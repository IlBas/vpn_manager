<UserControl x:Class="VpnManagerWPF.ClientControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VpnManagerWPF"
             mc:Ignorable="d" Width="75.941" Height="90.377">

    <Grid  Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="14*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Name="ClientName" Text="{Binding Path=Name}"/>
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0"  Name="Connect" IsEnabled="True" Background="Green"/>
            <Button Grid.Column="1"  Name="Disconnet" IsEnabled="False" Background="Red"/>
        </Grid>
    </Grid>
</UserControl>
